# ─────────────────────────────────────────────────────────────
# Streamlit Cloud requirements — dashboard ONLY
# Pure-Python wheels, no Rust/C compiler needed.
# Python 3.12 pinned via .python-version
# ─────────────────────────────────────────────────────────────

# Dashboard
streamlit==1.35.0
plotly==5.22.0
pandas==2.2.2
httpx==0.27.0

# Pydantic (used by dashboard component models)
pydantic==2.7.2

# Utilities
python-dotenv==1.0.1
